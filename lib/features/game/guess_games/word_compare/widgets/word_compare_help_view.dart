import 'package:flutter/material.dart';

class WordCompareHelpView extends StatelessWidget {
  const WordCompareHelpView({super.key});
  @override
  Widget build(BuildContext context) {
    return Expanded(child: Card());
  }
}
