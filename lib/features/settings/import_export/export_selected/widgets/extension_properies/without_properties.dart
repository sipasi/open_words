import 'package:flutter/material.dart';

class WithoutProperties extends StatelessWidget {
  const WithoutProperties({super.key});

  @override
  Widget build(BuildContext context) => Center(child: Text('Have no options'));
}
